# 🚀 Deploy ไปยัง Vercel (วิธีที่ถูกต้อง)

## 📍 จากหน้า "New Project" ที่เห็น:

### วิธีที่ 1: ใช้ Drag & Drop (ง่ายที่สุด)

1. **ดูที่ด้านล่างของหน้า** (scroll ลงไป)
   - มักจะมี section "**Deploy without Git**" หรือ "**Upload**"
   - หรือมีพื้นที่สำหรับ **ลากไฟล์มาวาง**

2. **ลากโฟลเดอร์ `flood-rescue-scout` มาวาง** ในพื้นที่นั้น

---

### วิธีที่ 2: ใช้ GitHub (ถ้ามี GitHub)

1. **คลิก "Continue with GitHub"**
2. **Authorize Vercel** (ถ้ายังไม่เคย)
3. **เลือก Repository** (ถ้ามี)
4. **หรือสร้าง Repository ใหม่:**
   - Push โค้ดไปยัง GitHub ก่อน
   - แล้วเลือก Repository

---

### วิธีที่ 3: ใช้ GitLab หรือ Bitbucket

- คลิก "Continue with GitLab" หรือ "Continue with Bitbucket"
- ทำตามขั้นตอนเดียวกัน

---

### วิธีที่ 4: ใช้ Vercel CLI (ถ้า login สำเร็จ)

```bash
cd flood-rescue-scout
vercel --yes
```

---

## 💡 Tips:

- **ถ้าไม่เห็น "Deploy without Git":**
  - ลอง **scroll ลงไป** ดูที่ด้านล่าง
  - หรือลอง **คลิกที่ input field** (อาจจะมี dropdown)
  - หรือลอง **ลากไฟล์มาวาง** ในพื้นที่ว่าง

- **ถ้ามี GitHub:**
  - ใช้ "Continue with GitHub" แล้วเลือก Repository
  - หรือสร้าง Repository ใหม่

---

## 🎯 ขั้นตอนที่แนะนำ:

1. **ลอง scroll ลงไป** ดูว่ามี section "Deploy without Git" หรือไม่
2. **หรือลองลากโฟลเดอร์ `flood-rescue-scout`** มาวางในหน้าเว็บ
3. **หรือใช้ GitHub:**
   - คลิก "Continue with GitHub"
   - สร้าง Repository ใหม่
   - Push โค้ด
   - Deploy

---

ลองทำตามขั้นตอนนี้ดูครับ!

