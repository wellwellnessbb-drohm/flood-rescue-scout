# Nginx Configuration Example for Flood Rescue Scout
# Copy this to your nginx config file

server {
    listen 80;
    server_name flood-rescue.yourdomain.com;  # เปลี่ยนเป็นโดเมนของคุณ
    
    # Redirect HTTP to HTTPS (แนะนำ)
    # return 301 https://$server_name$request_uri;
    
    root /var/www/flood-rescue;  # เปลี่ยนเป็น path ที่คุณ upload ไฟล์
    index dashboard-static.html index.html;
    
    # Serve Dashboard
    location / {
        try_files $uri $uri/ /dashboard-static.html;
    }
    
    # API endpoint - Proxy to Node.js server
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
        
        # CORS Headers
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type";
    }
    
    # Static files caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS Configuration (แนะนำ)
# server {
#     listen 443 ssl http2;
#     server_name flood-rescue.yourdomain.com;
#     
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
#     
#     # ... same configuration as above ...
# }

