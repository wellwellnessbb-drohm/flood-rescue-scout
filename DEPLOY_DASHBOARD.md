# วิธี Deploy Dashboard ให้คนอื่นเห็น

## 🎯 เป้าหมาย: ให้คนอื่นเข้าถึง Dashboard ได้

มีหลายวิธี:

## วิธีที่ 1: Deploy ไปยัง Vercel (แนะนำ - ฟรีและง่าย)

### ขั้นตอน:

1. **สร้างไฟล์ `vercel.json`:**
```json
{
  "version": 2,
  "builds": [
    {
      "src": "api-example.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/flood-rescue",
      "dest": "api-example.js"
    },
    {
      "src": "/dashboard.html",
      "dest": "dashboard.html"
    },
    {
      "src": "/",
      "dest": "dashboard.html"
    }
  ]
}
```

2. **ติดตั้ง Vercel CLI:**
```bash
npm install -g vercel
```

3. **Deploy:**
```bash
cd flood-rescue-scout
vercel
```

4. **แชร์ URL:** Vercel จะให้ URL เช่น `https://your-app.vercel.app`

## วิธีที่ 2: Deploy ไปยัง Netlify

1. **สร้างไฟล์ `netlify.toml`:**
```toml
[build]
  command = "echo 'No build needed'"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/dashboard.html"
  status = 200
```

2. **Deploy:**
- ไปที่ [Netlify](https://netlify.com)
- ลากโฟลเดอร์ `flood-rescue-scout` ไปวาง
- หรือใช้ Netlify CLI: `netlify deploy`

## วิธีที่ 3: ใช้ ngrok (สำหรับทดสอบ - ฟรี)

1. **ติดตั้ง ngrok:**
```bash
# ดาวน์โหลดจาก https://ngrok.com
# หรือใช้ npm
npm install -g ngrok
```

2. **รัน API Server:**
```bash
cd flood-rescue-scout
node api-example.js
```

3. **เปิด terminal ใหม่และรัน ngrok:**
```bash
ngrok http 3000
```

4. **คัดลอก URL ที่ได้** เช่น `https://abc123.ngrok.io`
5. **แชร์ URL นี้ให้คนอื่น**

## วิธีที่ 4: ใช้ Railway (แนะนำ - ฟรี)

1. ไปที่ [Railway](https://railway.app)
2. สร้างโปรเจกต์ใหม่
3. Deploy จาก GitHub หรือ upload โค้ด
4. ตั้งค่า PORT environment variable
5. Railway จะให้ URL อัตโนมัติ

## วิธีที่ 5: ใช้ Render (ฟรี)

1. ไปที่ [Render](https://render.com)
2. สร้าง Web Service ใหม่
3. Connect GitHub หรือ upload โค้ด
4. ตั้งค่า:
   - Build Command: `npm install`
   - Start Command: `node api-example.js`
5. Render จะให้ URL อัตโนมัติ

## วิธีที่ 6: ใช้ localhost + ngrok (เร็วที่สุด)

1. **รัน API Server:**
```bash
cd flood-rescue-scout
node api-example.js
```

2. **เปิด terminal ใหม่:**
```bash
ngrok http 3000
```

3. **คัดลอก URL** เช่น `https://abc123.ngrok.io`
4. **แชร์ URL นี้** - คนอื่นจะเข้าถึง Dashboard ได้ทันที

## วิธีที่ 7: Host บน Server จริง

ถ้าคุณมี server อยู่แล้ว:
1. Upload ไฟล์ทั้งหมดไปยัง server
2. รัน `node api-example.js` หรือใช้ PM2
3. ตั้งค่า reverse proxy (nginx) ถ้าต้องการ
4. แชร์ URL

## 📝 หมายเหตุ:

- **Vercel/Netlify:** ฟรี แต่ต้องแก้โค้ดให้รองรับ serverless
- **ngrok:** ฟรี ง่าย แต่ URL เปลี่ยนทุกครั้ง (เว้นแต่ใช้ plan ฟรี)
- **Railway/Render:** ฟรี tier มีให้ แต่มีข้อจำกัด

## 🚀 แนะนำ:

สำหรับการใช้งานจริง แนะนำ:
1. **Vercel** - ง่าย ฟรี รองรับ serverless
2. **Railway** - ง่าย deploy ได้ทันที
3. **ngrok** - สำหรับทดสอบเร็วๆ

