# วิธีใช้ API Endpoint (ง่ายกว่า Google Sheets!)

## 🎯 วิธีที่ง่ายที่สุด: ใช้ API Endpoint

### ขั้นตอน:

#### 1. เลือกวิธีสร้าง API:

**ตัวเลือก A: ใช้ไฟล์ตัวอย่างที่ให้มา**
- `api-example.js` (Node.js + Express)
- `api-example.py` (Python + Flask)

**ตัวเลือก B: ใช้ Serverless Function**
- Vercel
- Netlify Functions
- AWS Lambda

**ตัวเลือก C: ใช้ Webhook Service**
- Zapier
- Make.com (Integromat)
- n8n

#### 2. ตั้งค่า API Endpoint ใน Extension:

1. **Reload Extension:**
   - ไปที่ `chrome://extensions/`
   - คลิก Reload ที่ "Flood Rescue Scout"

2. **เปิด Extension popup**

3. **คลิกปุ่ม "🔗 ตั้งค่า API"** (ในส่วน Submitted Data Summary)

4. **ใส่ API Endpoint URL:**
   - ถ้าใช้ไฟล์ตัวอย่าง: `http://localhost:3000/api/flood-rescue`
   - ถ้าใช้ Vercel: `https://your-app.vercel.app/api/flood-rescue`
   - ถ้าใช้ Webhook: `https://hooks.zapier.com/hooks/catch/...`

5. **คลิก OK**

#### 3. ทดสอบ:

1. เปิด Facebook post
2. คลิก "Analyze Page"
3. กรอกข้อมูล
4. คลิก "Submit Data"
5. ดู Console ว่ามีข้อความ `✅ Synced to API successfully!`
6. ตรวจสอบ API ว่ามีข้อมูลเข้ามาหรือไม่

## 📝 ตัวอย่างการใช้งาน:

### ใช้ไฟล์ตัวอย่าง Node.js:

```bash
# ติดตั้ง dependencies
npm install express

# รัน server
node api-example.js

# Server จะรันที่ http://localhost:3000
# ตั้งค่าใน Extension: http://localhost:3000/api/flood-rescue
```

### ใช้ไฟล์ตัวอย่าง Python:

```bash
# ติดตั้ง dependencies
pip install flask flask-cors

# รัน server
python api-example.py

# Server จะรันที่ http://localhost:3000
# ตั้งค่าใน Extension: http://localhost:3000/api/flood-rescue
```

## 🔄 เปรียบเทียบกับ Google Sheets:

| คุณสมบัติ | API Endpoint | Google Sheets |
|----------|--------------|--------------|
| **ความยาก** | ⭐⭐ | ⭐⭐⭐ |
| **ต้องตั้งค่า** | ง่าย (แค่ URL) | ซับซ้อน (Apps Script) |
| **ยืดหยุ่น** | ✅ สูงมาก | ⚠️ จำกัด |
| **Auto-sync** | ✅ | ✅ |
| **Custom Logic** | ✅ ทำอะไรก็ได้ | ⚠️ จำกัด |

## 💡 ข้อดีของ API Endpoint:

1. **ง่ายกว่า:** แค่ใส่ URL
2. **ยืดหยุ่น:** ทำอะไรก็ได้ (Database, Email, SMS, etc.)
3. **ไม่ต้อง Deploy:** ใช้ localhost ก็ได้
4. **Custom Logic:** เพิ่ม validation, filtering, etc.

## 📚 ตัวอย่างเพิ่มเติม:

ดูไฟล์ `API_SETUP.md` สำหรับรายละเอียดเพิ่มเติม

